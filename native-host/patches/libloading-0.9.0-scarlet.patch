--- a/src/lib.rs
+++ b/src/lib.rs
@@ -60,6 +60,10 @@
 mod util;

 pub use self::error::Error;
+#[cfg(target_os = "scarlet")]
+mod scarlet;
+#[cfg(target_os = "scarlet")]
+pub use self::scarlet::{Library, Symbol};

 #[cfg(any(unix, windows, libloading_docs))]
 pub use self::safe::{Library, Symbol};
